# Single-agent Runbook

1. `docs/repo-map.md` と対象 task brief を読む
2. failing test が必要か判断する
3. code / docs / task artifacts を更新する
4. `python -m unittest discover -s tests -v` を実行する
5. progress note を更新する
